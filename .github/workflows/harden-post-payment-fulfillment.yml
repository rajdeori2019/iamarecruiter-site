name: Harden post-payment fulfillment

on:
  push:
    paths:
      - '.github/workflows/harden-post-payment-fulfillment.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Patch payment fulfillment state machine
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          p = Path('payments-bootstrap.js')
          text = p.read_text()

          new_block = r'''async function paymentVerifiedWithRetry_(payload, attempts = 3) {
  let lastErr;
  for (let attempt = 1; attempt <= attempts; attempt += 1) {
    try {
      return await trackerRequest('payment_verified', payload);
    } catch (err) {
      lastErr = err;
      const message = String(err && err.message || err);
      // A missing buyer is a state problem, not a transient transport problem.
      if (/Buyer order was not found in tracker/i.test(message)) throw err;
      console.warn('Tracker payment verification attempt ' + attempt + ' failed: ' + message);
      if (attempt < attempts) await new Promise((resolve) => setTimeout(resolve, 750 * attempt));
    }
  }
  throw lastErr || new Error('Tracker payment verification failed');
}

async function syncTrackerAfterPayment(order, paymentId, amount) {
  if (!trackerConfigured()) return { tracked: false, delivery_status: 'not_configured' };
  const orderId = String(order && order.id || '');
  const notes = order && order.notes ? order.notes : {};
  const coupon = String(notes.coupon || '');
  const whatsappGroupUrl = /^https:\/\//i.test(String(process.env.TMS_WHATSAPP_GROUP_URL || ''))
    ? String(process.env.TMS_WHATSAPP_GROUP_URL).trim()
    : '';
  const paymentPayload = {
    razorpay_order_id: orderId,
    razorpay_payment_id: paymentId,
    amount_paise: amount,
    coupon: coupon === 'none' ? '' : coupon,
    payment_status: 'CAPTURED'
  };

  // First try to mark the payment against an already-linked order. This is
  // idempotent and avoids creating duplicate buyer rows.
  try {
    await paymentVerifiedWithRetry_(paymentPayload);
  } catch (err) {
    const message = String(err && err.message || err);
    if (!/Buyer order was not found in tracker/i.test(message)) throw err;

    // The order row does not exist yet. Write it once. If Apps Script performs
    // the write but returns an HTML/non-JSON platform error, do NOT blindly
    // repeat the order write; the next payment_verified call tells us whether
    // the row actually exists and prevents duplicates.
    try {
      await trackerRequest('order_created', trackerOrderPayload_(order));
    } catch (orderErr) {
      console.warn('Tracker order write response was not confirmed: ' + String(orderErr && orderErr.message || orderErr));
    }

    await paymentVerifiedWithRetry_(paymentPayload);
  }

  // Delivery is intentionally attempted once because the current Apps Script
  // email action is not yet idempotent. A failed/unconfirmed response is
  // surfaced as pending instead of being silently swallowed or sending a
  // duplicate email.
  const delivery = await trackerRequest('send_delivery_email', {
    razorpay_order_id: orderId,
    razorpay_payment_id: paymentId,
    amount_paise: amount,
    whatsapp_group_url: whatsappGroupUrl
  });
  if (!delivery || delivery.email_sent !== true) throw new Error('Buyer delivery email did not confirm success.');
  return { tracked: true, delivery_status: 'sent' };
}

async function verifyPayment(req, res) {
  if (!allowed(req)) return json(res, 429, { error: 'Too many requests. Please try again shortly.' });
  try {
    const body = await readJson(req);
    const orderId = String(body.razorpay_order_id || '');
    const paymentId = String(body.razorpay_payment_id || '');
    const signature = String(body.razorpay_signature || '');
    if (!orderId || !paymentId || !signature) return json(res, 400, { error: 'Missing payment verification details.' });

    const { keySecret } = getCredentials();
    const expected = crypto.createHmac('sha256', keySecret).update(orderId + '|' + paymentId).digest('hex');
    if (!safeEqualHex(expected, signature)) return json(res, 400, { error: 'Payment signature verification failed.' });

    const purchase = await capturedPurchase(orderId, paymentId);
    const order = purchase.order;
    const coupon = order && order.notes ? String(order.notes.coupon || '') : '';
    let deliveryStatus = trackerConfigured() ? 'pending' : 'not_configured';

    if (trackerConfigured()) {
      try {
        const fulfillment = await syncTrackerAfterPayment(order, paymentId, purchase.amount);
        deliveryStatus = fulfillment && fulfillment.delivery_status ? fulfillment.delivery_status : 'pending';
      } catch (fulfillmentErr) {
        console.error('Post-payment fulfillment failed: ' + String(fulfillmentErr && fulfillmentErr.message || fulfillmentErr));
        deliveryStatus = 'pending';
      }
    }

    return json(res, 200, {
      verified: true,
      payment_id: paymentId,
      order_id: orderId,
      product: PRODUCT,
      amount: purchase.amount,
      currency: CURRENCY,
      coupon: coupon === 'none' ? '' : coupon,
      delivery_status: deliveryStatus
    });
  } catch (err) {
    console.error('Razorpay verification failed:', err.message);
    return json(res, 502, { error: 'Unable to verify payment right now. Please contact support with your payment ID.' });
  }
}

'''

          pattern = r"function syncTrackerAfterPayment\(order, paymentId, amount\) \{.*?\n\}\n\nasync function verifyPayment\(req, res\) \{.*?\n\}\n\n(?=function parseAccess)"
          patched, count = re.subn(pattern, new_block, text, count=1, flags=re.S)
          if count != 1:
              raise SystemExit('Expected fulfillment block not found exactly once; refusing to patch.')
          p.write_text(patched)
          PY

      - name: Validate JavaScript syntax
        run: node --check payments-bootstrap.js

      - name: Commit hotfix
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add payments-bootstrap.js
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Harden post-payment tracking and delivery"
          git push
